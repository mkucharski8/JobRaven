# Skopiuj jako .env i uzupełnij wartości. Plik .env nie jest commitowany.

# SMTP (poczta wychodząca – weryfikacja e-mail, reset hasła)
JOBRAVEN_SMTP_HOST=smtp.example.com
JOBRAVEN_SMTP_PORT=587
JOBRAVEN_SMTP_USER=twoj@email.com
JOBRAVEN_SMTP_PASS=twoje_haslo
JOBRAVEN_MAIL_FROM=twoj@email.com

# Gmail: host=smtp.gmail.com, port=587. Użyj hasła aplikacji (Konto Google → Bezpieczeństwo → Hasła aplikacji), nie zwykłego hasła.
# Gdy serwer SMTP ma certyfikat self-signed (np. dpoczta.pl): odblokuj wysyłkę
# JOBRAVEN_SMTP_INSECURE=1

# --- Mailgun API (zalecane na Railway – porty SMTP 587/465 są tam blokowane) ---
# W Mailgun: Sending → Domain → API Keys (Private API key) + nazwa domeny (np. sandboxXXX.mailgun.org)
# JOBRAVEN_MAILGUN_API_KEY=key-xxxxxxxxxxxx
# JOBRAVEN_MAILGUN_DOMAIN=sandboxXXXXXXXX.mailgun.org
# JOBRAVEN_MAIL_FROM=JobRaven <postmaster@sandboxXXXXXXXX.mailgun.org>

# --- Opcjonalnie: SMTP (np. lokalnie z dpoczta.pl lub innego serwera) ---
# JOBRAVEN_SMTP_HOST=smtp.mailgun.org
# JOBRAVEN_SMTP_PORT=587
# JOBRAVEN_SMTP_USER=postmaster@mg.twoja-domena.com
# JOBRAVEN_SMTP_PASS=haslo_z_mailgun
# JOBRAVEN_MAIL_FROM=noreply@twoja-domena.com

# Adres serwera (linki w mailach). W produkcji: https://twoja-domena.pl
# BASE_URL=http://localhost:3000

# Panel admina (/admin) – logowanie użytkownik + hasło. Aby ustawić inne hasło, wygeneruj hash:
# node -e "const c=require('crypto'); const s='jobraven-admin-2025'; console.log(c.scryptSync('TWOJE_NOWE_HASLO', s, 64).toString('hex'))"
# i ustaw: JOBRAVEN_ADMIN_PASSWORD_HASH=wygenerowany_hex
